name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [20.x]

    defaults:
      run:
        working-directory: Server  # All npm commands run inside /server

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}  # 👈 cache removed completely

      - name: Install dependencies
        run: npm ci

      # - name: Build project
      #   run: npm run build --if-present

      # - name: Run tests
      #   run: npm test
